package com.tts;

public class Main {

    public static void main(String[] args) {

/*
      // In if, the statements inside the if block will execute, if the condition is true and the control is passed to
      // the next statement after the if block. In the if else, if the condition is true, the statements inside the if
      // block will execute and if the condition is false the statements in the if else block will execute.
      // That is the key difference between if and if else.

        //this works, this is an if statement..........include Main class and main method
        int number = 70;

        if (number >= 50) {
            System.out.println("the number is higher than 50");
        }
        System.out.println("end of the program");

        // this ends the if statement explanation and code
*/


 /*     //  In if else, there are two blocks. The if statement contains an expression to evaluate. If the evaluated
      //  expression is true, then the statements inside the if block execute. At the end of the if block, the control
      //  is passed to the very next statement after the if block. If the expression is false, the control is passed to
      //  the else block and the statements of the else block execute. At the end of the else block, the control is
      //  passed to the next statement after the else block.

        // this works, an if else statement..........include Main class and main method
        int user = 17;
        if (user <= 18) {
            System.out.println("User is 18 or younger");
        }
        else {
            System.out.println("User is older than 18");
        }
        }
        // this ends if else statement explanation and code
 */

/*
    //The example below uses a do/while loop. The loop will always be executed at least once, even if the condition is
    //false, because the code block is executed before the condition is tested:
    //Do not forget to increase the variable used in the condition, otherwise the loop will never end! See below statement


   // this works, a do while loop..........include Main class and main method

        int c = 10;

    do {
        System.out.println("working" + c);
    }
    while (c < 10);         //you can also use <= in this example to show how the loop continues while condition is true
    }
    }
    // this ends do while loop explanation and code
*/
    }
}



